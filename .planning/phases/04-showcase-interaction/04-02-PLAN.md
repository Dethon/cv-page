---
phase: 04-showcase-interaction
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/Contact.tsx
  - src/components/contact.css
autonomous: true

must_haves:
  truths:
    - "Contact footer shows working email link to JF_Crespo@outlook.es"
    - "Contact footer shows working LinkedIn link with icon"
    - "Contact footer shows working GitHub link with icon"
    - "User can download CV as PDF with single click"
    - "Icon-only links have proper ARIA labels for screen readers"
  artifacts:
    - path: "src/components/Contact.tsx"
      provides: "Contact footer with icon links and PDF download"
      min_lines: 30
    - path: "src/components/contact.css"
      provides: "Footer layout and icon styling"
      contains: "contact"
  key_links:
    - from: "src/components/Contact.tsx"
      to: "src/data/cv-content.ts"
      via: "import personalInfo for contact data"
      pattern: "import.*personalInfo.*from.*cv-content"
    - from: "src/components/Contact.tsx"
      to: "react-icons"
      via: "import icon components"
      pattern: "from 'react-icons"
    - from: "src/components/Contact.tsx"
      to: "public/cv.pdf"
      via: "download link href"
      pattern: "cv\\.pdf"
---

<objective>
Replace the Contact placeholder component with a footer section containing social icon links (email, LinkedIn, GitHub) using react-icons and a one-click CV download button.

Purpose: Implements SHOW-02 (PDF download) and SHOW-03 (contact footer with icons).
Output: Working contact footer with accessible icon links and PDF download.
</objective>

<execution_context>
@/home/dethon/.claude/get-shit-done/workflows/execute-plan.md
@/home/dethon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-showcase-interaction/04-RESEARCH.md
@.planning/phases/04-showcase-interaction/04-01-SUMMARY.md
@src/components/Contact.tsx
@src/data/cv-content.ts
@src/styles/theme.css
@src/components/sections.css
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contact footer component with icons and download</name>
  <files>
    src/components/Contact.tsx
    src/components/contact.css
  </files>
  <action>
    1. Create src/components/contact.css:
       - `.contact-footer`: background var(--bg-secondary), border-top 1px solid var(--border), padding 3rem 1rem, text-align center
       - `.contact-content`: max-width 1200px, margin 0 auto
       - `.contact-content h2`: color var(--text-primary), margin-bottom 1.5rem (use h2 not h3 to match section heading pattern)
       - `.contact-links`: flex, justify-content center, gap 2rem, margin-bottom 2rem
       - `.contact-icon`: color var(--text-secondary), display inline-flex, transition color 0.2s ease and transform 0.2s ease
       - `.contact-icon:hover`: color var(--accent), transform scale(1.1) -- wrap in @media (hover: hover) to avoid sticky hover on mobile
       - `.download-btn`: inline-block, padding 0.75rem 1.5rem, background var(--accent), color var(--bg-primary), no text-decoration, border-radius 6px, font-weight 600, transition background 0.2s ease
       - `.download-btn:hover`: background var(--accent-hover)
       - Responsive: 768px+ padding 4rem 2rem, gap 3rem on contact-links

    2. Replace src/components/Contact.tsx placeholder:
       - Import `{ FaGithub, FaLinkedin }` from `'react-icons/fa'`
       - Import `{ MdEmail }` from `'react-icons/md'`
       - Import `{ personalInfo }` from `'../data/cv-content'`
       - Import `'./sections.css'` AND `'./contact.css'`
       - Keep `id="contact"` on the section element (scroll-spy needs it)
       - Use `section.section > div.container` wrapper matching existing pattern, BUT also add class `contact-footer` to the section for the footer-specific background/border styling
       - Render heading "Get in Touch"
       - Render contact-links div with three accessible icon links:
         a. Email: `href="mailto:${personalInfo.email}"`, `aria-label="Email JF_Crespo@outlook.es"`, MdEmail icon with `aria-hidden="true"` and size={28}
         b. LinkedIn: `href={personalInfo.linkedin}`, `target="_blank"`, `rel="noopener noreferrer"`, `aria-label="Visit LinkedIn profile"`, FaLinkedin icon with `aria-hidden="true"` and size={28}
         c. GitHub: `href={personalInfo.github}`, `target="_blank"`, `rel="noopener noreferrer"`, `aria-label="Visit GitHub profile"`, FaGithub icon with `aria-hidden="true"` and size={28}
       - Render download button as anchor: `href={import.meta.env.BASE_URL + 'cv.pdf'}`, `download="Juan_Francisco_Crespo_CV.pdf"`, text "Download CV (PDF)", class "download-btn"
       - Use `import.meta.env.BASE_URL` for the PDF path (resolves to `/cv-page/` in production) rather than hardcoding the base path. This ensures it works in both dev and production.

    CRITICAL accessibility pattern from research:
    - `aria-label` goes on the `<a>` tag (describes the destination)
    - `aria-hidden="true"` goes on the icon component (screen readers ignore the decorative icon)
    - Never leave icon-only links without aria-label
  </action>
  <verify>
    - `npm run build` succeeds
    - `npm run dev` and visually check the Contact section at bottom of page
    - Email link href is `mailto:JF_Crespo@outlook.es`
    - LinkedIn link opens in new tab to linkedin.com/in/jfcrespo5
    - GitHub link opens in new tab to github.com/dethon
    - Download CV button triggers PDF download
    - Icons render visually (not blank/broken)
    - `npm run preview` and verify PDF download works with base path (href includes /cv-page/)
  </verify>
  <done>
    Contact footer displays three icon links (email, LinkedIn, GitHub) with proper aria-labels and a Download CV button. All links point to correct destinations. PDF downloads successfully. Icons render from react-icons library.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Contact section renders at page bottom with icon links and download button
- Email link triggers mailto: with correct address
- LinkedIn and GitHub links open correct URLs in new tabs
- Download CV button triggers PDF download with filename "Juan_Francisco_Crespo_CV.pdf"
- Screen reader: each icon link has descriptive aria-label
- Icons are visible in both light and dark theme
- No sticky hover state on mobile (hover effects scoped with @media (hover: hover))
</verification>

<success_criteria>
Contact footer shows working email, LinkedIn, and GitHub icon links with proper accessibility labels. One-click PDF download works in both development and production environments. Component follows established project patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/04-showcase-interaction/04-02-SUMMARY.md`
</output>
