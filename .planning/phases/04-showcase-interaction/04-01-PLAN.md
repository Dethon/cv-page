---
phase: 04-showcase-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/content.ts
  - src/data/cv-content.ts
  - src/components/Projects.tsx
  - src/components/projects.css
  - public/cv.pdf
autonomous: true

must_haves:
  truths:
    - "Curated project cards display with title, description, technology tags, and GitHub link"
    - "Project cards have optional demo link that only appears when provided"
    - "Project cards are responsive across mobile, tablet, and desktop"
    - "react-icons library is installed for use by contact footer plan"
    - "CV PDF is available for download from public directory"
  artifacts:
    - path: "src/types/content.ts"
      provides: "ProjectEntry interface"
      contains: "interface ProjectEntry"
    - path: "src/data/cv-content.ts"
      provides: "Curated project data array and github URL on personalInfo"
      contains: "export const projects"
    - path: "src/components/Projects.tsx"
      provides: "Project card grid rendering"
      min_lines: 30
    - path: "src/components/projects.css"
      provides: "Card grid layout and responsive styles"
      contains: "projects-grid"
    - path: "public/cv.pdf"
      provides: "Static CV PDF for download"
  key_links:
    - from: "src/components/Projects.tsx"
      to: "src/data/cv-content.ts"
      via: "import projects array"
      pattern: "import.*projects.*from.*cv-content"
    - from: "src/components/Projects.tsx"
      to: "src/types/content.ts"
      via: "ProjectEntry type usage"
      pattern: "ProjectEntry"
---

<objective>
Extend the content data layer with project and GitHub data, install react-icons, move the CV PDF to public/, and replace the Projects placeholder component with a responsive card grid showing curated project entries.

Purpose: Implements SHOW-01 (project cards) and prepares shared dependencies for SHOW-02/SHOW-03 (PDF download, contact footer).
Output: Working project card section with real data, react-icons installed, CV PDF servable from public/.
</objective>

<execution_context>
@/home/dethon/.claude/get-shit-done/workflows/execute-plan.md
@/home/dethon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-showcase-interaction/04-RESEARCH.md
@src/types/content.ts
@src/data/cv-content.ts
@src/components/Projects.tsx
@src/components/sections.css
@src/styles/theme.css
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data layer, dependencies, and PDF setup</name>
  <files>
    src/types/content.ts
    src/data/cv-content.ts
    public/cv.pdf
    package.json
  </files>
  <action>
    1. Install react-icons: `npm install react-icons`

    2. Move CV PDF to public directory: `mv cv.pdf public/cv.pdf` (file exists at project root)

    3. Add ProjectEntry interface to src/types/content.ts:
       ```typescript
       export interface ProjectEntry {
         title: string;
         description: string;
         technologies: string[];
         githubUrl: string;
         demoUrl?: string;
       }
       ```

    4. Add `github` field to PersonalInfo interface in src/types/content.ts:
       ```typescript
       export interface PersonalInfo {
         name: string;
         title: string;
         summary: string;
         location: string;
         email: string;
         linkedin: string;
         github: string;  // NEW
       }
       ```

    5. Update src/data/cv-content.ts:
       - Add `github: "https://github.com/dethon"` to personalInfo object
       - Import ProjectEntry type
       - Add `export const projects: ProjectEntry[]` with 4-6 curated projects. Since the user hasn't specified exact projects, create representative placeholder entries based on the CV experience:
         - A data processing/ML project (reflects NielsenIQ work)
         - An audio/signal processing project (reflects Audaspace/Blender work)
         - A full-stack web application (reflects NTT DATA/Redslim work)
         - This CV portfolio page itself (meta project)
       - Use realistic but clearly placeholder descriptions that the user can easily update. Mark titles with real-sounding names (not "Project 1").
       - Each project should have 3-5 technology tags from the user's actual skill set.
       - Use `https://github.com/dethon` as base for githubUrl entries.
       - Include demoUrl on at least one project (e.g., this CV page: `https://dethon.github.io/cv-page`).
  </action>
  <verify>
    - `npm run build` succeeds (TypeScript compiles with new types)
    - `ls public/cv.pdf` confirms PDF exists in public directory
    - `grep "ProjectEntry" src/types/content.ts` shows interface exists
    - `grep "github" src/data/cv-content.ts` shows github field populated
  </verify>
  <done>
    ProjectEntry interface defined, personalInfo has github field, projects array contains 4-6 curated entries, react-icons installed, cv.pdf in public/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Project cards component and styles</name>
  <files>
    src/components/Projects.tsx
    src/components/projects.css
  </files>
  <action>
    1. Create src/components/projects.css with responsive card grid layout:
       - `.projects-grid`: CSS Grid with `grid-template-columns: repeat(auto-fit, minmax(280px, 1fr))`, gap 1.5rem
       - `.project-card`: background var(--bg-secondary), border 1px solid var(--border), border-radius 8px, padding 1.5rem, flex column layout with gap 1rem
       - Hover effect wrapped in `@media (hover: hover)` to avoid sticky hover on mobile: translateY(-4px) and box-shadow 0 4px 12px var(--shadow)
       - `.project-title`: 1.25rem, var(--text-primary), margin 0
       - `.project-description`: var(--text-secondary), line-height 1.6, flex-grow 1 (pushes links to bottom)
       - `.project-tags`: flex wrap with 0.5rem gap
       - `.tech-tag`: inline-block, padding 0.25rem 0.75rem, font-size 0.875rem, border-radius 12px, background var(--accent), color var(--bg-primary), font-weight 500
       - `.project-links`: flex with 1rem gap, margin-top auto (aligns to card bottom)
       - `.project-link`: color var(--accent), no text-decoration, font-weight 500, hover underline
       - Responsive breakpoints: 768px (minmax 320px), 1200px (minmax 350px)

    2. Replace src/components/Projects.tsx placeholder with full implementation:
       - Import `projects` from `../data/cv-content`
       - Import `./projects.css` (replace `./sections.css` import -- keep importing sections.css too for .section and .container classes)
       - Keep `id="projects"` on the section element (scroll-spy needs it)
       - Use `section.section > div.container` wrapper matching existing pattern
       - Map over projects array to render cards
       - Each card: h3 title, p description, div.project-tags with tech-tag spans, div.project-links with GitHub link (always) and optional Demo link
       - External links use `target="_blank" rel="noopener noreferrer"`
       - Use index as key (static list, no reordering)

    Follow the exact patterns from 04-RESEARCH.md code examples. Reference existing component patterns from Hero.tsx and Experience.tsx for section structure.
  </action>
  <verify>
    - `npm run build` succeeds
    - `npm run dev` and visually check /cv-page/#projects shows card grid
    - Cards display title, description, tech tags, and links
    - Cards respond to viewport changes (1 column mobile, 2-3 columns desktop)
  </verify>
  <done>
    Projects section shows responsive card grid with curated project data. Each card has title, description, technology tags as badges, GitHub link, and optional demo link. Hover effects work on desktop, no sticky hover on mobile.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Projects section renders card grid with real data (not placeholder text)
- Cards are responsive: single column on mobile (320px), multi-column on desktop (1200px)
- Tech tags display as styled badges
- GitHub links open in new tab
- Optional demo links only appear on projects that have them
- `public/cv.pdf` exists and is accessible
- `react-icons` appears in package.json dependencies
</verification>

<success_criteria>
Project cards display curated entries with description, technology tags, GitHub links, and optional demo links in a responsive CSS Grid layout. Data layer includes ProjectEntry type and github field on PersonalInfo. react-icons installed and CV PDF available in public directory.
</success_criteria>

<output>
After completion, create `.planning/phases/04-showcase-interaction/04-01-SUMMARY.md`
</output>
