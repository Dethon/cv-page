---
phase: 03-core-content
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/Skills.tsx
  - src/components/skills.css
  - src/components/Education.tsx
  - src/components/education.css
autonomous: true

must_haves:
  truths:
    - "Skills section shows 6 categories: Frontend, Backend, Data and ML, Systems and Audio, DevOps, Languages"
    - "Each skill category displays its technologies as tag-style items"
    - "Skills grid is 1 column on mobile, 2 on tablet, 3 on desktop"
    - "Education section lists all 3 degrees from Universidad de Salamanca"
    - "Each degree shows the degree name and year range"
  artifacts:
    - path: "src/components/Skills.tsx"
      provides: "Skills section with categorized grid layout"
      min_lines: 25
    - path: "src/components/skills.css"
      provides: "Skills grid and tag styles"
      contains: ".skills-grid"
    - path: "src/components/Education.tsx"
      provides: "Education section with degree listings"
      min_lines: 20
    - path: "src/components/education.css"
      provides: "Education card styles"
      contains: ".education-list"
  key_links:
    - from: "src/components/Skills.tsx"
      to: "src/data/cv-content.ts"
      via: "imports skillCategories"
      pattern: "import.*skillCategories.*from.*data/cv-content"
    - from: "src/components/Education.tsx"
      to: "src/data/cv-content.ts"
      via: "imports education"
      pattern: "import.*education.*from.*data/cv-content"
---

<objective>
Implement the Skills and Education sections with categorized skill tags and degree listings.

Purpose: Skills demonstrates Juan Francisco's technical breadth across 6 categories, while Education shows his formal academic background. Together they complete the four core content sections of the CV.
Output: Fully styled Skills grid with tag-style items and Education section with degree cards.
</objective>

<execution_context>
@/home/dethon/.claude/get-shit-done/workflows/execute-plan.md
@/home/dethon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-content/03-RESEARCH.md
@.planning/phases/03-core-content/03-01-SUMMARY.md
@src/styles/theme.css
@src/components/sections.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Skills section with categorized grid</name>
  <files>src/components/Skills.tsx, src/components/skills.css</files>
  <action>
Update `src/components/Skills.tsx` to:

1. Import `skillCategories` from `../data/cv-content`
2. Import `./skills.css` and keep `./sections.css`
3. Semantic HTML structure:
   - `<section id="skills" className="section" aria-labelledby="skills-heading">`
   - `<h2 id="skills-heading">Skills</h2>`
   - `<div className="skills-grid">`
   - Each category: `<div className="skill-category">`
     - `<h3>` with category name
     - `<ul className="skill-list">` (unordered, skills have no ranking)
     - Each skill: `<li className="skill-tag">` with skill name

Create `src/components/skills.css`:

Use ONLY existing CSS custom properties from theme.css.

Grid layout:
- `.skills-grid`: display grid, grid-template-columns 1fr (mobile), gap 2.5rem 2rem, margin-top 2rem
- Tablet (768px): grid-template-columns repeat(2, 1fr)
- Desktop (1200px): grid-template-columns repeat(3, 1fr)

Category cards:
- `.skill-category h3`: font-size 1.15rem, color var(--accent), margin-bottom 1rem, font-weight 600

Skill tags:
- `.skill-list`: display flex, flex-wrap wrap, gap 0.5rem, list-style none, padding 0, margin 0
- `.skill-tag`: padding 0.4rem 0.85rem, background var(--bg-secondary), border 1px solid var(--border), border-radius 6px, font-size 0.875rem, color var(--text-secondary), white-space nowrap

Do NOT add hover effects or animations. Phase 5 handles polish.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Run `npm run build` — builds. Verify all 6 categories render with their skills as tags.</verify>
  <done>Skills section shows 6 categories in a responsive grid with tag-style skill items. Grid is 1 column mobile, 2 tablet, 3 desktop.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Education section with degree listings</name>
  <files>src/components/Education.tsx, src/components/education.css</files>
  <action>
Update `src/components/Education.tsx` to:

1. Import `education` from `../data/cv-content`
2. Import `./education.css` and keep `./sections.css`
3. Semantic HTML structure:
   - `<section id="education" className="section" aria-labelledby="education-heading">`
   - `<h2 id="education-heading">Education</h2>`
   - `<ul className="education-list">` (unordered list, degrees are not ranked)
   - Each degree: `<li className="education-item">`
     - `<article className="education-card">`
     - `<h3 className="education-degree">` with degree name
     - `<p className="education-institution">` with institution name
     - `<p className="education-period">` with `{startYear} - {endYear}`

Create `src/components/education.css`:

Use ONLY existing CSS custom properties from theme.css.

Education list:
- `.education-list`: list-style none, padding 0, margin 2rem 0, display grid, gap 1.5rem
- Tablet (768px): grid-template-columns repeat(2, 1fr) — but since there are 3 items, let them wrap naturally or keep single column. Use grid-template-columns 1fr on mobile, and at 768px use repeat(auto-fill, minmax(300px, 1fr)) for flexible layout.

Education cards:
- `.education-card`: padding 1.5rem, background var(--bg-secondary), border-radius 8px, border 1px solid var(--border)
- `.education-degree`: font-size 1.15rem, color var(--text-primary), margin-bottom 0.5rem, font-weight 600
- `.education-institution`: font-size 1rem, color var(--accent), margin-bottom 0.25rem
- `.education-period`: font-size 0.9rem, color var(--text-secondary)

Do NOT add hover effects or animations. Keep it clean and static.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Run `npm run build` — builds. Verify all 3 degrees render with correct details.</verify>
  <done>Education section lists all 3 degrees from Universidad de Salamanca with degree name, institution, and year range in styled cards. Responsive layout works across breakpoints.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` completes successfully
- Skills shows 6 categories: Frontend, Backend, Data and ML, Systems and Audio, DevOps, Languages
- Each skill category has correct technology tags
- Skills grid responsive: 1col (320px), 2col (768px), 3col (1200px)
- Education shows 3 degrees: Master's, Bachelor's, Technical Engineering
- All entries show Universidad de Salamanca with correct year ranges
- Dark/light theme toggle works for both sections
</verification>

<success_criteria>
- Skills grid displays 6 categories with tag-style items
- Responsive grid columns adjust per breakpoint (1, 2, 3 columns)
- Education shows all 3 degrees with institution and dates
- Both sections use semantic HTML (section, h2, ul/li, article)
- Theme toggle maintains correct colors
- No placeholder text remains in either component
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-content/03-03-SUMMARY.md`
</output>
