---
phase: 03-core-content
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/Experience.tsx
  - src/components/experience.css
autonomous: true

must_haves:
  truths:
    - "Experience section shows all 6 companies in chronological order (most recent first)"
    - "Each position displays company name, role title, date range, and location"
    - "NTT DATA and GRIAL each show their 2 distinct roles"
    - "Achievement bullet points are visible for every role"
    - "A vertical timeline visually connects all positions"
    - "Dates are formatted as human-readable month and year (e.g., 'April 2023')"
  artifacts:
    - path: "src/components/Experience.tsx"
      provides: "Experience timeline rendering all career positions"
      min_lines: 40
    - path: "src/components/experience.css"
      provides: "Timeline layout and experience card styles"
      contains: ".timeline"
  key_links:
    - from: "src/components/Experience.tsx"
      to: "src/data/cv-content.ts"
      via: "imports experiences array"
      pattern: "import.*experiences.*from.*data/cv-content"
    - from: "src/components/Experience.tsx"
      to: "src/types/content.ts"
      via: "uses formatDate helper"
      pattern: "import.*formatDate.*from.*types/content"
---

<objective>
Implement the Experience timeline section showing all career positions with a vertical timeline layout.

Purpose: Displays Juan Francisco's career progression across 6 companies (8 roles) in a visually connected timeline, which is the most content-rich section of the CV and the primary way visitors understand his professional breadth.
Output: Fully styled Experience component with vertical timeline, company cards, role details, and achievement bullets.
</objective>

<execution_context>
@/home/dethon/.claude/get-shit-done/workflows/execute-plan.md
@/home/dethon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-content/03-RESEARCH.md
@.planning/phases/03-core-content/03-01-SUMMARY.md
@src/styles/theme.css
@src/components/sections.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Experience component with vertical timeline</name>
  <files>src/components/Experience.tsx</files>
  <action>
Update `src/components/Experience.tsx` to:

1. Import `experiences` from `../data/cv-content` and `formatDate` from `../types/content`
2. Import `./experience.css` and keep `./sections.css`
3. Use semantic HTML structure:
   - `<section id="experience" className="section" aria-labelledby="experience-heading">`
   - `<h2 id="experience-heading">Experience</h2>`
   - `<ol className="timeline">` (ordered list for chronological data)
   - Each experience entry is `<li className="timeline-item">`

4. For each ExperienceEntry, render an `<article className="experience-card">`:
   - `<h3 className="experience-company">` with company name
   - For each role in the `roles` array:
     - `<div className="experience-role">`
     - `<h4 className="experience-title">` with role title
     - `<div className="experience-meta">`:
       - `<time dateTime="{startDate}">` formatted start date
       - " - "
       - If endDate is 'Present': `<span>Present</span>`, otherwise `<time dateTime="{endDate}">` formatted end date
       - ` · ` then location
     - `<ul className="experience-achievements">` with each achievement as `<li>`

5. For companies with multiple roles (NTT DATA, GRIAL), all roles render under the single company heading within the same timeline item. Add a visual separator between roles within the same card (a subtle top border on subsequent roles using CSS `.experience-role + .experience-role`).

6. Use `key` props: company name for `<li>`, index for roles within a company, index for achievements.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Run `npm run build` — builds successfully.</verify>
  <done>Experience component renders all 6 companies with 8 total roles, proper semantic HTML, and accessible timeline markup.</done>
</task>

<task type="auto">
  <name>Task 2: Style the Experience timeline</name>
  <files>src/components/experience.css</files>
  <action>
Create `src/components/experience.css` with mobile-first vertical timeline styles.

Use ONLY existing CSS custom properties from theme.css: var(--accent), var(--bg-secondary), var(--text-primary), var(--text-secondary), var(--border), var(--bg-primary).

Timeline structure:
- `.timeline`: list-style none, padding 0, margin 2rem 0, position relative
- `.timeline::before`: vertical connector line — position absolute, left 20px, top 0, bottom 0, width 2px, background var(--border)
- `.timeline-item`: position relative, padding-left 60px, margin-bottom 3rem
- `.timeline-item::before`: circle marker — position absolute, left 11px, top 4px, width 20px, height 20px, border-radius 50%, background var(--bg-primary), border 3px solid var(--accent), z-index 1

Experience card:
- `.experience-card`: no extra background (inherits section background), clean layout
- `.experience-company`: font-size 1.35rem, color var(--text-primary), margin-bottom 0.75rem
- `.experience-title`: font-size 1.1rem, color var(--text-primary), font-weight 500, margin-bottom 0.25rem
- `.experience-meta`: font-size 0.9rem, color var(--text-secondary), margin-bottom 0.75rem
- `.experience-meta time, .experience-meta span`: no special styling (inherits)
- `.experience-achievements`: margin 0, padding-left 1.25rem, list-style disc
- `.experience-achievements li`: color var(--text-secondary), line-height 1.6, margin-bottom 0.25rem

Multi-role separator:
- `.experience-role + .experience-role`: margin-top 1.25rem, padding-top 1.25rem, border-top 1px solid var(--border)

Tablet (768px):
- `.timeline-item`: padding-left 70px
- `.experience-card`: max-width 700px

Desktop (1200px):
- `.timeline::before`: left 24px
- `.timeline-item::before`: left 15px
- `.timeline-item`: padding-left 80px

Do NOT add animations. Phase 5 handles polish and animations. Keep it static and clean.
  </action>
  <verify>Run `npm run dev` and visually inspect: vertical line connects all entries, circle markers align with the line, company names are prominent, dates are readable, achievements are bulleted. Check at 320px, 768px, and 1200px widths. Verify dark mode renders correctly (border colors, text colors adapt).</verify>
  <done>Experience timeline displays with vertical connector line, circle markers for each company, card layout with company/role/dates/achievements, responsive across all breakpoints, and correct dark/light theme colors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` completes successfully
- Experience section shows exactly 6 companies: Redslim, NielsenIQ, NTT DATA Europe and LATAM, GRIAL, Audaspace, Universidad de Salamanca
- NTT DATA shows 2 roles (Full Stack Engineer, Back End Engineer)
- GRIAL shows 2 roles (Software Engineer, Software Developer)
- All dates formatted as "Month Year" (e.g., "April 2023")
- Vertical timeline line and circle markers render correctly
- Responsive at 320px, 768px, 1200px
- Dark/light theme toggle works
</verification>

<success_criteria>
- All 6 companies and 8 roles are visible with correct data
- Timeline has visual vertical line connecting positions
- Each position shows company, title, dates, location, and achievements
- Semantic HTML uses ol, li, article, time elements
- Responsive layout works across all breakpoints
- Theme toggle maintains correct colors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-content/03-02-SUMMARY.md`
</output>
